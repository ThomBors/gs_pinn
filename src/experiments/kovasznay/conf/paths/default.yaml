# path to root directory
root_dir: ${oc.env:PROJECT_ROOT}

data_path: ${hydra:runtime.cwd}/data/kovasznay/

out_path: ${hydra:runtime.cwd}/results/kovasznay/${hydra:runtime.version}/outputs
shp_path: ${hydra:runtime.cwd}/results/kovasznay/${hydra:runtime.version}/shp
ll_path: ${hydra:runtime.cwd}/results/kovasznay/${hydra:runtime.version}/losslandscape

checkpoint_path: ${hydra:runtime.cwd}/checkpoints/kovasznay

# path to logging directory
log_dir: ${paths.root_dir}/logs/train/

# path to working directory
work_dir: ${hydra:runtime.cwd}

